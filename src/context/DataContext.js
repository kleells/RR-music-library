// file created to represent the context defualt vlaue used in createContext()

import React, { createContext } from 'react'

// variable that stores the context Data array
export const DataContext = createContext([])
